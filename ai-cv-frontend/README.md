Frontend structure for AI CV Tailoring Tool

Current endpoint: http://localhost:5173/

For node.js to run please ensure you are in the the ai-cv-frontend folder,
and then use the command npm run dev to launch the local host in the browser.

To view live updates just copy and paste the end point into the browser, once node.js is active.